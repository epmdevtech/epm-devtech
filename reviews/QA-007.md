# QA-007 — LCP/FCP Mobile 100

| Especificação | SPEC-007 |
| ------------- | -------- |
| Autor         | Gemini/Antigravity |
| Data          | 2026-08-31 |
| Status        | ✅ Aprovado |

## Validações Realizadas

1. **Visibilidade Imediata do h1 (LCP)**: Removidos `opacity-0` e `animationDelay` do `h1` em `Hero.tsx`. Ele renderiza de forma imediata sem animação de fade, eliminando a penalidade no Lighthouse.
2. **Srcset com Descritores w**: Atualizado `Header.tsx` de descritores de densidade (`1x`, `2x`) para descritores de largura `149w`, `300w` com `sizes="(max-width: 640px) 83px, 95px"`, garantindo que o `xs.webp` (3 KiB) será baixado.
3. **Preload e HTML estático**: Foi incluído o preload `<link rel="preload" as="image" href="/logo-emp-dev-tech-xs.webp" fetchpriority="high" />` no `<head>` e injetado o HTML estático de Hero diretamente em `<div id="root">` no arquivo `index.html` para zerar o tempo até o FCP.
4. **Testes e Build**:
   - `npm run test`: Passando perfeitamente.
   - `npm run build`: Finalizado sem erros.

O projeto está pronto para a verificação PageSpeed no Vercel.
