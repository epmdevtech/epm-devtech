# QA-044 — Proxy Reverso Odontologia Demo via Vercel

**SPEC:** SPEC-044  
**TASK:** TASK-044  
**Data:** 2026-09-22  
**Agente:** Gemini/Antigravity  

---

## Quality Gates

| Gate | Critério | Status |
|------|---------|--------|
| JSON válido | `vercel.json` sem erros de sintaxe | ✅ Passou |
| Ordem de rewrites | Regras proxy antecedem a regra SPA | ✅ Passou |
| Rewrite base | `/odontologia-demo` → Worker | ✅ Presente |
| Rewrite path | `/odontologia-demo/:path*` → Worker com `:path*` | ✅ Presente |
| Header `X-Content-Type-Options` | `nosniff` | ✅ Presente |
| Header `X-Frame-Options` | `SAMEORIGIN` | ✅ Presente |
| Header `Referrer-Policy` | `strict-origin-when-cross-origin` | ✅ Presente |
| Header `Permissions-Policy` | `camera=(), microphone=(), geolocation=()` | ✅ Presente |
| Preservação SPA | Rewrite `/(.*) → /index.html` intacto | ✅ Preservado |
| Escopo | Nenhum arquivo fora do escopo modificado | ✅ Conforme |

---

## Validação de Sintaxe

```
python3 -c "import json; json.load(open('vercel.json'))"
Saída: JSON válido ✅ | Rewrites: 3 | Header sources: ['/odontologia-demo/:path*']
```

---

## Validação Funcional (pós-deploy)

> Os itens abaixo devem ser verificados pelo PO após o próximo deploy na Vercel.

| Teste | Esperado |
|-------|---------|
| `GET https://epmdevtech.com.br/odontologia-demo` | Resposta 200 com conteúdo do Worker |
| `GET https://epmdevtech.com.br/odontologia-demo/qualquer/path` | Resposta 200 proxiada com path preservado |
| Cabeçalho `X-Content-Type-Options` na resposta | `nosniff` |
| Cabeçalho `X-Frame-Options` na resposta | `SAMEORIGIN` |
| Cabeçalho `Referrer-Policy` na resposta | `strict-origin-when-cross-origin` |
| Cabeçalho `Permissions-Policy` na resposta | `camera=(), microphone=(), geolocation=()` |
| `GET https://epmdevtech.com.br/` | SPA carrega normalmente (rewrite SPA preservado) |
| Cookies `epmdevtech.com.br` | Não encaminhados ao Worker |

---

## Resultado

**✅ APROVADO** — Todos os quality gates de implementação passaram.  
Validação funcional pendente de deploy pelo PO.

---

_Agente: Gemini/Antigravity | 2026-09-22_
